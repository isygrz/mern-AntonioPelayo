// Loaded automatically before every E2E spec.
// Use this file to import custom commands and set global config.

import './commands';

// You can set global before/after hooks here if desired.
// Example: clear footer snapshot before each test (optional)
// beforeEach(() => {
//   cy.clearLocalStorage('snapshot:footer:v1');
// });
