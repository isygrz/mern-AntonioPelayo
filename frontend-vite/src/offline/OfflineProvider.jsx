// Backward-compat re-export so existing imports from '@offline/OfflineProvider'
// resolve to the hardened provider in '@contexts/OfflineProvider'.
export { default, useOffline } from '@/contexts/OfflineProvider';
